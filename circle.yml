machine:
  services:
    - docker

dependencies:
  pre:
    - gradle wrapper
    - gradle build
  override:
    - docker info
    - docker build -t peerdavid/hellojava .

deployment:
  docker:
    branch:
      master
    commands:
      docker push peerdavid/hellojava